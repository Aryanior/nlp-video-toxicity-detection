# nlp-video-toxicity-detection
An end-to-end NLP application that detects toxic speech in videos by extracting audio, converting speech to text, preprocessing with NLTK, generating TF-IDF features, and classifying toxicity using a LightGBM model. Built with Python, SpeechRecognition, MoviePy, and Gradio.
